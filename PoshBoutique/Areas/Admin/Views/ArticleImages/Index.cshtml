@model IEnumerable<PoshBoutique.Data.ArticleImage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SmallImageUrl)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MediumImageUrl)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LargelImageUrl)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrderIndex)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Article.UrlName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SmallImageUrl)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MediumImageUrl)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LargelImageUrl)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrderIndex)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Article.UrlName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
